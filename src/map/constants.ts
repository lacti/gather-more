import Position from "./models/Position";
export const redisMapKey = `map-simple`;
export const redisLockKey = `lock-basic`;

export const maxY = 10;
export const maxX = 10;

export const invalidPosition: Position = { x: -1, y: -1 };
